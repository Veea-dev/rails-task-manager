<h1>These are all tasks</h1>
<ul>
  <% @tasks.each do |task| %>
    <li>
      <%= link_to task.title, task_path(task)%>
    </li>
  <% end %>
</ul>

<p><%= link_to 'Create new task', new_task_path(@task)%></p>
